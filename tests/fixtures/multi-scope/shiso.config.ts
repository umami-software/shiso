// Bare default export: the fixture has no node_modules, so it cannot import
// the optional defineConfig helper from @umami/shiso/config.
export default {
  siteUrl: 'https://fixture.example.com',
};
