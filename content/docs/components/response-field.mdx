---
title: Response field
description: Document object properties and schema fields.
---

Use `ResponseField` for response body fields, object properties, and similar
schema documentation. Nest fields under headings to document nested objects.

## Basic usage

<ResponseField name="id" type="string" required>
  Unique identifier for the resource.
</ResponseField>

```tsx
<ResponseField name="id" type="string" required>
  Unique identifier for the resource.
</ResponseField>
```

## Optional and deprecated fields

<ResponseField name="legacyField" type="number" deprecated>
  Removed in a future release.
</ResponseField>

```tsx
<ResponseField name="legacyField" type="number" deprecated>
  Removed in a future release.
</ResponseField>
```

## Properties

<PropertiesTable>
<ResponseField name="name" type="string" required>
  Field name.
</ResponseField>

<ResponseField name="type" type="string | number | boolean | null">
  Type label. HTML entities in string values are decoded before rendering.
</ResponseField>

<ResponseField name="required" type="boolean" default="false">
  Shows a required badge.
</ResponseField>

<ResponseField name="deprecated" type="boolean" default="false">
  Shows a deprecated badge.
</ResponseField>

<ResponseField name="children" type="ReactNode">
  Field description.
</ResponseField>
</PropertiesTable>
