import { defineConfig } from '@umami/shiso/config';

export default defineConfig({
  // Set your production origin so canonical URLs, Open Graph tags, and the
  // sitemap are generated.
  // siteUrl: 'https://docs.example.com',
});
